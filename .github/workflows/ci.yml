name: CI Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. 取出程式碼
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 設定 Python 環境（用穩定版）
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # 3. 安裝套件
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install setuptools  # <--- 重點！補上這行
          pip install pytest pytest-cov coverage-badge black
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      # 4. 程式碼格式檢查 (black)
      - name: Run Black (code style check)
        run: black --check .

      # 5. 測試 (pytest + coverage)
      - name: Run tests
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)/src
          pytest --cov=src -v

      # 6. 生成 Coverage Badge
      - name: Generate coverage badge
        run: |
          # 確保先執行測試產生 .coverage 檔案
          pytest --cov=.
          # 使用 python -m 來執行，更穩定
          python -m coverage_badge -o coverage.svg -f
